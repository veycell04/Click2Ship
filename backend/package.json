{
  "name": "click2ship-backend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/local.ts",
    "build": "node scripts/clean.mjs && tsc -p tsconfig.json",
    "start": "node dist/src/local.js",
    "db:migrate": "tsx src/scripts/migrate.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@easypost/api": "^8.9.0",
    "@fastify/cors": "^11.3.0",
    "fastify": "^5.6.0",
    "pg": "^8.22.0",
    "stripe": "^22.4.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/pg": "^8.20.4",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0",
    "vitest": "^4.0.0"
  }
}
